apiVersion: apps/v1
kind: Deployment
metadata:
    name: runner-scheduler
    namespace: kube-system
    labels:
        component: runner-scheduler
spec:
    replicas: 1
    selector:
        matchLabels:
            component: runner-scheduler
    template:
        metadata:
            labels:
                component: runner-scheduler
        spec:
            serviceAccountName: runner-scheduler
            imagePullSecrets:
              - name: your-secret
            containers:
                - name: scheduler
                  image: your-image:your-tag
                  imagePullPolicy: Always
                  command:
                      - /bin/kube-scheduler
                      - --config=/etc/kubernetes/scheduler-config.yaml
                      - --v=3
                  volumeMounts:
                      - name: config
                        mountPath: /etc/kubernetes
            volumes:
                - name: config
                  configMap:
                      name: runner-scheduler-config
